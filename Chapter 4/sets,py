e = set()#no repetition allowed
s = {1,4,5,6,45,3,2}
print(s)

# set can have strings also
print(s, type(s))
s.add(566)
# sets are unorderded
# sets are unindexed 
# there is no way to change the items in sets
# cannot contain duplicate values 
# Operationb In Sets
len(s)
s.remove(4)
s.pop()
s.union()
s.intersection(s2)#overlapping values